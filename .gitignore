node_modules/
gui/node_modules/
preview-deepseek-code/node_modules/

dist/
build/
coverage/
.vite/
gui/renderer-dist/
gui/__screenshots__/

*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

.env
.env.*
!.env.example

.DS_Store
Thumbs.db

.tmp-*/
.tmp/
tmp/

# ── 本地 / 运行时状态(不入库;注意 .gitignore 不支持行尾注释)──
# 运行时:会话日志、本地 config.json(含 API Key)、GUI 偏好
.deepseek-code/
# Claude Code 本地状态:settings.local.json、worktrees 等
.claude/
# 编辑器本地配置
.vscode/
.idea/
# Superpowers/SDD 执行期临时文件(任务简报、报告、进度账本)
.superpowers/

# GUI mockup renders + throwaway capture scripts (keep the .html baseline)
gui/mockups/*.png
gui/mockups/_capture*.js
